{% extends 'base.html.twig' %}
{% block body %}

<div class="container mt-5">
	<div class="container card mb-3 shadow-lg bg-dark text-light" style="max-width: 540px;">
		<div class="row g-0">
			<div class="col-md-4">
				<img src="{{products.picture}}" class="img-fluid rounded-start" alt="...">
			</div>
			<div class="col-md-8">
				<div class="card-body">
					<h5 class="card-title">{{products.name}}</h5>
					<p class="card-text">{{products.description}}</p>
					<p class="card-text">{{products.fk_manufacturer.name}}</p>
					<p class="card-text">Manufacturer: {{products.fk_manufacturer.country}}</p>
					<p class="card-text">Price: {{products.fk_price.price}} €</p>
					<p class="card-text">
						<small class="text-muted">Last updated 3 mins ago</small>
						<a href="/edit/{{products.id}}" class="btn-grad3">Edit</a>
						<a href="/zalando" class="btn-grad">Back</a>
						<a href="/delete/{{products.id}}" onclick="return confirm('Are you sure you want to delete this?')" class="btn-grad2">Delete</a>
					</p>
				</div>
			</div>
		</div>
	</div>
</div>
< {% endblock %}
